import { ReactElement } from "react";

interface SectionContainerComponentProps {
    twHeight?: string;
    title: string;
    children: ReactElement | ReactElement[];

}

export default SectionContainerComponentProps;